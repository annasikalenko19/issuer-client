# Issuer Admin Client

A browser client for reviewing credential issuance requests. The app lists pending requests, opens request details, and lets an issuer administrator approve or reject a request.

## Features

- View credential requests in a table.
- Open a request detail page.
- Approve credential requests.
- Reject credential requests with a required rejection reason.